"use client";
import { useState } from "react";
import styles from "./state.module.css";

export function StateElement({ argen }) {
  return;
}

export function State({ country }) {
  // Reacting to input with state
}
